services:
  api:
    build: .
    ports:
      - "8000: 8000"
    volumes:
      - ./models:/app/models
      - ./assets:/app/assets
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
